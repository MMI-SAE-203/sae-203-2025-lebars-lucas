---
import Cardfilms from '../components/Cardfilms.astro';
import Layout from '../layouts/Layout.astro';
import { SortallFilms } from '../../backend/backend.mjs';

const Films = await SortallFilms();
---

<Layout title="La Sélection Horrifique">
<body>

  <h1 class="text-4xl font-bold text-orange-500 text-center my-8">
    Plongez dans l'angoisse : Le planning de Festiv'Halloween
</h1>
    <main class="container mx-auto p-4 flex justify-center pb-10">
        {Films.length > 0 ? (
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {Films.map((Films) => (
              <Cardfilms {...Films} />
            ))}
          </div>
        ) : (
          <p class="text-gray-600 text-center">
            Aucune programmation disponible pour le moment.
          </p>
        )}
      </main>
</body>
</Layout>